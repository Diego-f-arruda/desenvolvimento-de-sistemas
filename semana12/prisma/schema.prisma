generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Task {
  id        String   @id
  text      String
  completed Boolean
  createAt  DateTime
  updateAt  DateTime @updatedAt

  @@map("tasks")
}

model User {
  id        String   @id
  name      String
  email     String   @unique
  password  String
  birthDate DateTime
  createAt  DateTime
  updateAt  DateTime @updatedAt

  @@map("users")
}
